## Source

- Specification: `docs/specifications/slice-a-engineering-specification.md` §10 row 11; contracts §3 W6, §8 (pins); §5.
- Grouping: `docs/specifications/slice-a-execution-grouping-amendment.md` row R4 (row 11; no in-ticket checkpoints).
- Deciding records: T15 #10 d15 (planning lifecycle); T20 #22 d1–d5 (CI plan contract and PR classes); T06 #8 d4 (authorization binding); §8.1 (toolchain and action pins); §8.2 (reproducibility, `GEN-DRIFT`).

## What to build

### Row 11 — CI workflow files (validate, plan-preview, lab-plan, lab consumption) using exactly the §8 pins

The workflow files implementing the `validate` and `plan-preview` checks, the post-merge `lab-plan` job, the `lab` environment consumption rule, the two-layer classifier integration, the effective-access summary generation, and the authorization binding (T06 #8 d4) — authored with exactly the §8.1 pins. Cross-ticket prerequisites (spec edges 11 ← 3, 11 ← 8, 11 ← 9, 11 ← 10) are satisfied before this ticket starts via the native chain R4 ← R3 ← R2 ← R1 (amendment §3 rows #13–#16).

- Scope: workflow files only — **authoring, not activation**. Server-side activation of the control set (ruleset, required checks, environments) is row 14a (ticket R6), separately authorized. Until R6/R7 complete, the `lab-plan` and `lab` jobs are necessarily inert: no roles, no environments, no apply path exists.
- Acceptance criteria:
  - [ ] Every action referenced exclusively by its full 40-hex SHA from §8.1 (tag as trailing comment); adding an action is a platform-change PR.
  - [ ] Toolchain execution happens inside the pinned container; runner `ubuntu-24.04` is transport only, with the actual runner-image identity recorded in run evidence (§8.1).
  - [ ] `validate` and `plan-preview` run for reviewed PRs; `plan-preview` output is sanitized, non-authoritative, never eligible for apply (T15 d15).
  - [ ] The post-merge `lab-plan` job is defined to run from the exact `main` source commit and produce the encrypted applicable saved plan and digest; the `lab` consumption rule binds exactly that saved plan, source commit, inventory snapshot, and digest; any input change invalidates the authorization (T15 d15).
  - [ ] Regeneration determinism is enforced as a CI check (`GEN-DRIFT`, §8.2); identical commands local and CI (§8.2).
  - [ ] No workflow can apply anything in this ticket's scope; no secret or live identifier appears in any workflow file.
- Evidence: PR record with the three review axes and Quality Gate verdict; static verification of every pin against §8.1; procedural-interval records (server-side checks cannot yet be required).

## Exclusions

Server-side GitHub configuration (R6); AWS roles/bootstrap (R7); lab remediation (R8); any workflow execution against AWS.

## Ordered checkpoints

Single row — none. Cross-ticket ordering is carried entirely by the native edge R4 ← R3.

## Closeout rule

One closeout covers row 11 only when its acceptance criteria and required verification evidence pass (execution-grouping amendment §1).

## Lifecycle and review

Row 11 lands through the decision-gated implementation lifecycle S7–S11: `implement` (feature branch; never self-merges) → `codebase-design` (validate mode) → `tdd` → `code-review` with the Standards + Spec + Conformance axes → `code-review` as the independent Quality Gate; merge only at ⟦G-Merge⟧ by the repository owner. Unratified seams or questions HALT to excursion E1. During the recorded T15 d15 procedural interval (before R6/14a activation), every implementation PR keeps PR/merge records and validation evidence procedurally.

## Authorization boundaries

Feature branches only; owner merges. No AWS contact or mutation; no Terraform execution; **no GitHub configuration** — authoring workflow files is repository content, activation is R6 under its own authorization. No Terraform apply may occur before the R6/14a control set is active (T15 d15). Nothing in this ticket authorizes R6, R7, or R8.

## Empirical gates

Provider execution remains NOT RUN/BLOCKED and unverified until executed in the designated lab-CI boundary — nothing in this ticket executes it. The `forget`-representation and import-redaction conditions remain open and unadvanced. No claim of closure (§8.3).

## Blocked by

- {{R3}} (R3 — Plan analysis and generated governance)
