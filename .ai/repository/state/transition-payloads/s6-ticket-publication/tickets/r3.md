## Source

- Specification: `docs/specifications/slice-a-engineering-specification.md` §10 rows 9, 10, 12; contracts §3 W6, W7; §5 (testing decisions).
- Grouping: `docs/specifications/slice-a-execution-grouping-amendment.md` row R3 (rows 9, 10, 12; in-ticket checkpoint edges #8–#9).
- Deciding records per sub-item below. On any divergence, the cited record prevails.

## What to build

The plan-analysis and generated-governance layer: the two-layer plan-effect classifier, the effective-access generator with its two renderings, and the generated CODEOWNERS, views, and central generated-artifact manifest.

## Sub-items (original §10 rows; ordered checkpoints)

### Row 9 — Plan-effect classifier (two layers) + `CLS-*` (§3 W6)

Checkpoint position: first in this ticket — cross-ticket prerequisite (spec edge 9 ← 3) satisfied by this ticket's native blocker chain (R3 ← R2).

- Records: T20 #22 (plan contract and PR classes); T15 #10 (deployment contract); §8.3 (the `forget`-representation condition).
- Scope: the two-layer plan-effect classifier and its `CLS-*` codes, tested through synthetic plan-JSON fixtures covering every action class, both replace orders, `forget` patterns, and fail-closed divergence (§5). The pinned-provider `forget` representation is unverified: `state-removal-only` cannot activate and `CLS-REPRESENTATION` fails closed — this row implements the fail-closed behavior without advancing the condition.
- Acceptance criteria:
  - [ ] Every action class, both replace orders, and the `forget` patterns are covered by synthetic fixtures with expected classifications.
  - [ ] Divergence between layers fails closed; `state-removal-only` cannot activate; `CLS-REPRESENTATION` fires as recorded.
- Evidence: red-first TDD record over the synthetic plan-JSON fixture set; no fixture is ever planned against AWS.

### Row 10 — Effective-access generator (preview + summary) + `GEN-*` checks (§3 W6, W7)

Checkpoint position: after row 9 (amendment §3 edge #8: 10 ← 9).

- Records: T20 #22 d1–d5 (sanitized non-authoritative PR preview; digest-bound applicable summary); T15 #10 d12 (alias-only public artifacts); §5 (byte-determinism); §8.2 (`GEN-DRIFT` regeneration determinism).
- Scope: one generator, two renderings — the sanitized, non-authoritative plan-preview and the digest-bound effective-access summary with per-control enforcement evidence — plus the `GEN-*` checks.
- Acceptance criteria:
  - [ ] Identical inputs produce identical bytes (byte-determinism proven in tests).
  - [ ] Both renderings derive from the one generator; the preview is sanitized and never eligible for apply; public artifacts are alias-only with zero live identifiers.
- Evidence: byte-determinism test record; regeneration comparison.

### Row 12 — Generated CODEOWNERS + views + manifest (§3 W7)

Checkpoint position: after row 10 (amendment §3 edge #9: 12 ← 10). The additional specification edge 12 ← 8 (governance registry) is a cross-ticket prerequisite satisfied before this ticket starts via the native chain R3 ← R2 ← R1 (amendment §3 row #12).

- Records: T20 #22 d6 (generated-artifact metadata contract); §3 W7 (generated only, never hand-authored); §5 (generated-vs-regenerated CI comparison).
- Scope: the three views, generated CODEOWNERS (from the row-8 registry and routing), the enforcement-evidence report, and the central generated-artifact manifest under the T20 #22 d6 metadata contract.
- Acceptance criteria:
  - [ ] Every generated artifact carries the d6 metadata and appears in the central manifest; none is hand-authored.
  - [ ] CODEOWNERS derives deterministically from the registry/routing; regeneration yields identical bytes.
- Evidence: generated-vs-regenerated comparison record; review-axis confirmation that no generated artifact was hand-edited.

## Exclusions

CI workflow files and their integration of these components (R4); Terraform (R5); all live activation (R6–R8). Nothing here contacts AWS, runs Terraform, or configures GitHub.

## Ordered checkpoints

Row 9 → Row 10 → Row 12 (amendment §3 edges #8–#9). A blocked row's acceptance criteria may not be evaluated as passing before its blocking row's criteria pass.

## Closeout rule

One closeout covers rows 9, 10, and 12 only when every row's acceptance criteria and required verification evidence pass together; any incomplete row keeps this ticket open (execution-grouping amendment §1).

## Lifecycle and review

Each row lands through the decision-gated implementation lifecycle S7–S11: `implement` (feature branch; never self-merges) → `codebase-design` (validate mode where the specification pre-settled structure) → `tdd` (red-first record kept; ratified seams only) → `code-review` with the Standards + Spec + Conformance axes → `code-review` as the independent Quality Gate; merge only at ⟦G-Merge⟧ by the repository owner. Unratified seams or questions HALT to excursion E1 (halt-don't-decide). During the recorded T15 d15 procedural interval (before the R6/14a control set is active), every implementation PR keeps PR/merge records and validation evidence procedurally.

## Authorization boundaries

Feature branches only; owner merges. No AWS contact or mutation; no Terraform execution; no GitHub configuration. Nothing in this ticket authorizes R6, R7, or R8.

## Empirical gates

The `forget`-representation condition governs row 9's fail-closed behavior and remains open and unadvanced; provider execution and import redaction likewise remain open. No row claims any condition closed (§8.3).

## Blocked by

- {{R2}} (R2 — Validation and catalog system)
