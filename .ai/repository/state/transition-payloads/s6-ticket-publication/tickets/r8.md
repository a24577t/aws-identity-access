## Source

- Specification: `docs/specifications/slice-a-engineering-specification.md` §10 row 15; §6 (out of scope).
- Grouping: `docs/specifications/slice-a-execution-grouping-amendment.md` row R8 (row 15; no in-ticket checkpoints).
- Deciding records: T16 #11 d11 (stop-with-remediation disposition — Stages 0–6, each step separately authorized; per-stage verifications); brownfield implementation plan §1 (sequencing prerequisite and per-stage verification criteria); T15 #10 d12 (three-tier evidence, alias-only public artifacts).

**This ticket is separately authorized beyond its creation — and separately per stage: every stage below requires its own explicit Eric authorization at execution time. Creating or claiming this issue authorizes nothing. Lab-remediation authorization is never combined with AWS-bootstrap authorization (R7).**

## What to execute

### Row 15 — Lab remediation Stages 0–6, exactly as T16 #11 decision 11 defines them

The staged lab remediation, summarized by plan §1 as: informed signup gate (Stage 0); management-account verification and Organization gate (Stage 1); lab OU and sandbox invitation with billing verification (Stage 2); workload accounts (Stage 3); payer-level zero-spend budget and Free Tier alerts before Identity Center (Stage 4); Identity Center, first group, delegated administrator (Stage 5); read-only re-discovery before the binding record, bootstrap, or any plan (Stage 6). The T16 #11 d11 record is authoritative for every stage's exact content, gates, and verifications; nothing is restated here.

- Scope: each stage executed only under its own separate authorization, exactly per T16 d11, with its read-only, non-public verifications and its present-consequences gates (account plan, credits, payment readiness, billing responsibility, Paid-Plan conversion risks) performed before each authorization point. No authorization below is implied by any prior stage's completion.
- Acceptance criteria:
  - [ ] Every executed stage meets its T16 d11 per-stage verification plus the engineering specification's per-work-item acceptance (plan §1).
  - [ ] All public artifacts are alias-only; live identifiers (account IDs, root e-mails, OU paths) are confined to encrypted non-public evidence (T15 d12).
  - [ ] No payment details or credentials are recorded anywhere (T16 d11 1.1).
  - [ ] Stage 6's read-only re-discovery completes before the binding record, bootstrap, or any plan consumes the topology.
- Evidence: per-stage evidence records under the T15 d12 tier model; the stage-authorization trail (one explicit authorization per stage); repository-side records landed through the lifecycle below.

## Exclusions

The import rehearsal and target waves (post-acceptance, separately authorized — plan §4/§5; specification §6); any Terraform apply (gated by R6/14a activation and R7); anything the three empirical conditions gate.

## Ordered checkpoints

Single row — none. Cross-ticket ordering is carried by the native edge R8 ← R7.

## Closeout rule

One closeout covers row 15 only when every executed stage's criteria and evidence pass together (execution-grouping amendment §1); any incomplete stage keeps this ticket open.

## Lifecycle and review

Every live step is executed by Eric under its own per-stage authorization; no agent contacts AWS or mutates anything. Repository-side records and evidence land through the decision-gated implementation lifecycle S7–S11 with the Standards + Spec + Conformance axes and the independent Quality Gate; merge only at ⟦G-Merge⟧ by the repository owner.

## Authorization boundaries

Separately authorized beyond ticket creation, and separately authorized per stage. No agent AWS contact or mutation; no Terraform execution; no GitHub configuration. **No Terraform apply may occur before the R6/14a control set is active** (T15 d15) — and no stage authorization implies any apply.

## Empirical gates

Provider execution, `forget` representation, and import redaction remain open and unadvanced; no stage advances or claims any of them (§8.3). No rehearsal activity of any kind occurs before the import-redaction gate passes with empirical evidence (plan §4).

## Blocked by

- {{R7}} (R7 — AWS-side OIDC/bootstrap)
