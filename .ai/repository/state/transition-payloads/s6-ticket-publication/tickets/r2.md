## Source

- Specification: `docs/specifications/slice-a-engineering-specification.md` §10 rows 3, 4, 5, 6, 7; contracts §3 W3, W4; §5 (testing decisions); §7 (catalog data); §8.1 (toolchain pins).
- Grouping: `docs/specifications/slice-a-execution-grouping-amendment.md` row R2 (rows 3–7; in-ticket checkpoint edges #3–#7).
- Deciding records per sub-item below. On any divergence, the cited record prevails.

## What to build

The complete validation and catalog system: the validator core and finding contract, the full non-ADM code catalogue, the ADM detector, the digest-pinned ADM catalog data generated from the committed SRI sources, and the governed fixture tree that proves all of it externally.

## Sub-items (original §10 rows; ordered checkpoints)

### Row 3 — Validator core + finding contract + severity/stage model (§3 W3)

Checkpoint position: first in this ticket — cross-ticket prerequisite (spec edge 3 ← 2) satisfied by this ticket's native blocker R1.

- Records: T14 #19 (canonical triggering layers, stage attribution, hermetic validation boundary, harmonization rules, finding-record contract with C14 total ordering and duplicate collapse, leak rule with its single ARN exemption); §9 C-B anchor corrections where T14 anchors are translated.
- Scope: the `src/` validator core everything else plugs into, under the §8.1 pins exactly (container `python:3.12.7-slim@sha256:60d9996b…`, `PyYAML == 6.0.2`, `jsonschema == 4.23.0`, `pip 24.2` / `pip-tools 7.4.1`); the hash-locked `requirements.txt` generated inside the pinned container lands in the same PR as the first `src/` code (§8.1).
- Acceptance criteria:
  - [ ] Finding records implement C14 total ordering and duplicate collapse, externally observable and deterministic.
  - [ ] Severity/stage model, canonical triggering layers, and harmonization rules follow T14 #19.
  - [ ] The validation boundary is hermetic — no network, no AWS, no environment-dependent behavior; the leak rule is enforced with exactly its single ARN exemption.
  - [ ] Identical commands produce identical results locally and in CI, both inside the pinned container (§8.2).
- Evidence: red-first TDD record; external-behavior tests only (§5); recorded hermetic run inside the pinned container.

### Row 4 — Catalogue families INV/PRQ/P-OOS/KEY/ASN/GOV/FIX/CFG/DOC (§3 W3)

Checkpoint position: after row 3 (edge #3: 4 ← 3).

- Records: T14 #19 (the 79-code catalogue: stable codes, severities, triggering layers, stage attribution); accepted ADR IDs join catalogue citations at S6 (T08 #13 d11).
- Scope: every non-ADM code of the catalogue on the row-3 core.
- Acceptance criteria:
  - [ ] Every code of the nine families implemented; none added, renamed, or dropped relative to T14 #19.
  - [ ] Each code fires at its canonical layer with its catalogue severity and stage attribution; isolatable codes are deterministically isolatable.
  - [ ] The approved multi-code expectation `{ASN-SHAPE, P-OOS-USER}` behaves exactly as recorded.
- Evidence: red-first TDD record with expected finding sets (§5).

### Row 5 — ADM detector (rules 1/3/5 executable; 2/4 data-blocked) + `ADM-CATALOG` (§3 W3, §7)

Checkpoint position: after row 3 (edge #4: 5 ← 3).

- Records: T14 #19 d5 (rules 2/4 executable only when the catalog files exist with matching digests); T21 #20 (rules 2/4 content); ADR-0008 (no standing administrator access).
- Scope: ADM rules 1/3/5 executable now; rules 2/4 data-blocked, failing closed as `ADM-CATALOG` until row 6's catalogs exist with matching digests; a wildcard whose prefix is outside the catalog is an expansion inability → `ADM-CATALOG` (fail closed).
- Acceptance criteria:
  - [ ] Rules 1/3/5 executable; the approved multi-code expectation `{ADM-CAPABLE, ADM-STANDING}` behaves exactly as recorded.
  - [ ] Rules 2/4 fail closed as `ADM-CATALOG` on catalog absence, digest mismatch, unsupported schema, or expansion inability — never guess.
- Evidence: red-first TDD record covering both the executable rules and the fail-closed paths.

### Row 6 — Catalog data production from the committed §7 sources (§7)

Checkpoint position: after row 5 (edge #5: 6 ← 5).

- Records: specification §7 in full (7.1 byte inventory; 7.2 service scope; 7.3 deterministic transformation; 7.4 canonical outputs); `governance/catalogs/sources/sri-20260828/SOURCES.md` handling note; the repository byte-identity amendment (hash verification operates on committed Git-blob bytes; `*.json -text` keeps blob and checkout bytes identical).
- Scope: generate `governance/catalogs/action-catalog.json` and `governance/catalogs/privileged-mutation-actions.json` **only** from the committed `governance/catalogs/sources/sri-20260828/` files — each file's §7.1 SHA-256 verified against the committed blob bytes **before any parse**; no re-fetch; no review-staging consumption; mismatch aborts (fail closed). Transformation per §7.3 exactly (mutation iff `IsWrite ∨ IsPermissionManagement ∨ IsTaggingOnly`; a missing or non-boolean annotation aborts naming the action; never name-heuristics). Outputs in §7.4 canonical form with the sidecar metadata record; the catalog digest pinned in the validator's committed catalog reference; rules 2/4 thereby become executable.
- Acceptance criteria:
  - [ ] Pre-parse §7.1 hash verification implemented against blob bytes and demonstrated to abort on mismatch.
  - [ ] Generated catalogs are byte-deterministic (§7.4 canonical form; regeneration yields identical bytes) and their digests are pinned in the validator's catalog reference.
  - [ ] §7.2 scope exactly — included `iam`, `sso`, `identitystore`, `sso-directory`; exclusions unchanged.
- Evidence: regeneration-determinism demonstration; recorded hash-verification pass over the five committed sources.

### Row 7 — Fixture tree (valid + per-code invalid) (§3 W4)

Checkpoint position: after rows 4 and 5 (edges #6: 7 ← 4; #7: 7 ← 5).

- Records: T14 #19 d6 (fixture expectation-set pattern); §5 (no fixture ever contacts AWS, is planned against AWS, uploaded, or applied).
- Scope: `tests/fixtures/{valid,invalid/<CODE>}/` — one deterministic negative fixture per active isolatable code; exactly the two approved multi-code expectations (`{ASN-SHAPE, P-OOS-USER}`, `{ADM-CAPABLE, ADM-STANDING}`); synthetic clock-controlled plan/apply fixtures; the valid tree passing with zero findings.
- Acceptance criteria:
  - [ ] Valid tree → zero findings; every active isolatable code has exactly its deterministic negative fixture.
  - [ ] Expectation sets verified externally (expected finding sets, never internals); alias-only content throughout.
- Evidence: full fixture-suite run record inside the pinned container.

## Exclusions

Plan-effect classifier and generator (R3); CI workflows (R4); Terraform (R5); all live activation (R6–R8). Nothing here contacts AWS, runs Terraform, or configures GitHub; catalog generation never fetches from the network.

## Ordered checkpoints

Row 3 → rows 4 and 5; row 5 → row 6; rows 4 + 5 → row 7 (amendment §3 edges #3–#7). A blocked row's acceptance criteria may not be evaluated as passing before its blocking rows' criteria pass.

## Closeout rule

One closeout covers rows 3–7 only when every row's acceptance criteria and required verification evidence pass together; any incomplete row keeps this ticket open (execution-grouping amendment §1).

## Lifecycle and review

Each row lands through the decision-gated implementation lifecycle S7–S11: `implement` (feature branch; never self-merges) → `codebase-design` (validate mode where the specification pre-settled structure) → `tdd` (red-first record kept; ratified seams only) → `code-review` with the Standards + Spec + Conformance axes → `code-review` as the independent Quality Gate; merge only at ⟦G-Merge⟧ by the repository owner. Unratified seams or questions HALT to excursion E1 (halt-don't-decide). During the recorded T15 d15 procedural interval (before the R6/14a control set is active), every implementation PR keeps PR/merge records and validation evidence procedurally.

## Authorization boundaries

Feature branches only; owner merges. No AWS contact or mutation; no Terraform execution; no GitHub configuration; no network fetch in catalog generation. Regenerating the dependency lock or refreshing SRI sources afterward are platform-change PRs (§8.1, §7), out of scope. Nothing in this ticket authorizes R6, R7, or R8.

## Empirical gates

The three open empirical conditions (provider execution; `forget` representation; import redaction) remain open, unadvanced, and are never claimed closed (§8.3). Nothing in rows 3–7 activates them; ADM rules 2/4 activation via row 6 is a data condition, not an empirical condition.

## Blocked by

- {{R1}} (R1 — Foundation and contracts)
