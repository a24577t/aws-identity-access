## Source

- Specification: `docs/specifications/slice-a-engineering-specification.md` §10 row 14b; sequencing rules (correction-4 reconciliation).
- Grouping: `docs/specifications/slice-a-execution-grouping-amendment.md` row R7 (row 14b; no in-ticket checkpoints).
- Deciding records: T15 #10 d15 (AWS-side bootstrap under separate authorization; document 09's documented-bootstrap rule, cited at the pinned aws_ami revision `5f3cb7163f468730fd2ceb5d565c90b0bfda6099`, never restated — T08 #13 d3); T15 #10 d6 (roles, exact trust conditions, access models); T15 #10 d7 (permitted mutations, denies, protected resources); ADR-0009.

**This ticket is separately authorized beyond its creation. Creating or claiming this issue authorizes nothing; the one-time bootstrap apply below is executed locally by Eric under his own explicit authorization.**

## What to execute

### Row 14b — AWS-side OIDC/bootstrap (roles, trust policies, one-time bootstrap root apply)

The one-time `infrastructure/bootstrap` root applied locally by Eric under separate authorization (T15 d15), creating the two repository-specific pipeline roles in `role-host`, with outputs imported and evidenced per document 09's documented-bootstrap rule. Terraform prerequisites exist first (row 13 merged — carried by the native edge R7 ← R5).

- Scope: the **plan role** and **apply role** with trust policies binding the exact audience `sts.amazonaws.com`, the exact repository, and the exact environment subjects — plan role ← `repo:a24577t/aws-identity-access:environment:lab-plan`; apply role ← `repo:a24577t/aws-identity-access:environment:lab` (T15 d6). The existing GitHub OIDC provider in `role-host` is **referenced, never managed** (owned by `mcp_gateway01`). Pipeline roles cannot modify any role, trust policy, or OIDC provider. Access models and explicit denies exactly per T15 d6/d7; protected resources per ADR-0009.
- Sequencing (carried verbatim from the specification): 14b is independently and separately authorized when its Terraform prerequisites exist — 14a and 14b are **not** one atomic dependency; **no Terraform apply may occur before the item-14a control set is active** (T15 d15's planning lifecycle).
- Acceptance criteria:
  - [ ] Both roles exist with exactly the recorded trust conditions and access models; every explicit deny of T15 d7 present; the OIDC provider unmodified.
  - [ ] Bootstrap outputs imported and evidenced per document 09's documented-bootstrap rule; live identifiers confined to encrypted non-public evidence; public records alias-only (T15 d12).
  - [ ] The apply was one-time, local, Eric-executed, against the row-13 root, and no pipeline identity performed or can perform role/trust/OIDC mutation afterward.
  - [ ] Pre-apply evidence shows the R6/14a control set was verified active (server-state capture) before the bootstrap apply began — the mandatory execution gate below.
- Evidence: tier-model evidence per T15 d12 (alias-only public summary; encrypted non-public record of live identifiers); post-bootstrap read-only verification of role trust and policies; repository-side records landed through the lifecycle below.

## Exclusions

Repository-side control activation (R6 — its own separate authorization); lab remediation Stages 0–6 (R8 — each stage separately authorized; **AWS bootstrap authorization is never combined with lab-remediation authorization**); any Identity Center mutation; any apply of the `identity-center` root.

## Ordered checkpoints and execution gate

Single row — no in-ticket row checkpoints; cross-ticket ordering is carried by the native edge R7 ← R5. **Mandatory execution gate** (not an additional native dependency edge — the accepted graph is unchanged, and R6/R7 remain non-atomic):

- Preparation (authoring, review coordination, evidence planning) may proceed once R5 is closed.
- The one-time Terraform bootstrap apply may not begin until the R6/14a control set is verified active, evidenced from captured server state.
- This ticket cannot close until that pre-apply condition is evidenced in the closeout record.

## Closeout rule

One closeout covers row 14b only when its acceptance criteria and evidence set pass (execution-grouping amendment §1). An increment without its complete evidence set is not accepted regardless of live outcome.

## Lifecycle and review

The live bootstrap is executed by Eric under his own separate authorization; no agent executes Terraform or contacts AWS. Repository-side records and evidence land through the decision-gated implementation lifecycle S7–S11 with the Standards + Spec + Conformance axes and the independent Quality Gate; merge only at ⟦G-Merge⟧ by the repository owner.

## Authorization boundaries

Separately authorized beyond ticket creation. The sequencing rules above are carried verbatim and their application timing is the owner's decision at authorization. No agent AWS contact or Terraform execution under any circumstance; no GitHub configuration.

## Empirical gates

Provider execution remains open: it is unverified until executed in the designated lab-CI boundary, and this owner-executed local bootstrap makes no claim against that condition (§8.3). The `forget`-representation and import-redaction conditions remain open and unadvanced.

## Blocked by

- {{R5}} (R5 — Terraform roots and resource model)
