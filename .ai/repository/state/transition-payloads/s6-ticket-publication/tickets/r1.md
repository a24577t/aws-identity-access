## Source

- Specification: `docs/specifications/slice-a-engineering-specification.md` §10 rows 1, 2, 8; contracts §3 W1, W2, W5; §9 (C-A canonical keys).
- Grouping: `docs/specifications/slice-a-execution-grouping-amendment.md` row R1 (rows 1, 2, 8; in-ticket checkpoint edges #1–#2).
- Deciding records per sub-item below. On any divergence, the cited record prevails.

## What to build

The foundation a requester lands in: the accepted repository skeleton with the slice-A `access/` content, the JSON Schemas that make every governed form machine-checkable, and the governance registry with its routing and declaration schemas.

## Sub-items (original §10 rows; ordered checkpoints)

### Row 1 — Repository skeleton, root README sections, `access/` slice files (§3 W1)

Checkpoint position: first — no in-ticket blocker.

- Records: ADR-0003 (requester surface and top-level layout); T04 #6 d1/d6 (roots and README sections); T10 #15 d6 (assignment identity/filename rules); T05 #7 d2 (group reference form) and its identifier bounds; T22 #21 d1 (`configuration/instance.yml` without a `verification` block until first verification); §9 C-A canonical keys.
- Scope: the eight governed product roots and root README sections exactly as ADR-0003 / T04 #6 d1/d6 define them (product roots only — repository-authority, methodology, navigation, Git-control, and metadata paths are outside this count and untouched); `access/` holding exactly the five slice files (two permission sets using the §9 canonical keys — `inventory-reader`, 16 chars, within the T05 #7 bounds — and three assignments per T10 #15 d6); `groups/lab-readers.yml` in the T05 d2 reference form; `configuration/instance.yml` with no `verification` block. The real `lab-readers` group key is confirmed only at remediation Stage 5.2 (T16 #11 d11) — no AWS verification here.
- Acceptance criteria:
  - [ ] All eight ADR-0003 governed product roots exist exactly as named — `access/`, `governance/`, `docs/`, `schemas/`, `infrastructure/`, `src/`, `tests/`, `.github/` — and no additional product/domain root is introduced. Established repository-authority, methodology, navigation, Git-control, and metadata paths (`.ai/`, root navigation files, `.gitattributes`, and the like) are outside the product-root count and remain untouched.
  - [ ] The root README carries the T04 #6 d6 sections.
  - [ ] `access/` holds exactly the five slice files with §9 canonical keys and T10 #15 d6 filenames.
  - [ ] `groups/lab-readers.yml` follows the T05 d2 form; `configuration/instance.yml` has no `verification` block.
  - [ ] No committed file contains a live AWS identifier (account ID, ARN, root e-mail, live OU path) — aliases only (T15 #10 d5/d12).
- Evidence: PR record with the three review axes and Quality Gate verdict; file inventory checked one-to-one against §3 W1.

### Row 2 — JSON Schemas for all governed forms (§3 W2)

Checkpoint position: after row 1 (amendment §3 edge #1: 2 ← 1).

- Records: the accepted configuration contract (`docs/architecture/configuration-contract.md`, `status: accepted`); RD-04 at the pinned aws_ami revision `5f3cb7163f468730fd2ceb5d565c90b0bfda6099` (cited, never restated — T08 #13 d3); T06 #8 (declaration kinds).
- Scope: JSON Schemas under `schemas/` for the four requester forms, the lab inventory fixture, the governance records, and the three declaration kinds — implementing the configuration contract exactly; the schema is the contract's executable form and the contract prevails on divergence.
- Acceptance criteria:
  - [ ] Every governed form named by the configuration contract has a schema; no field added, renamed, or relaxed.
  - [ ] The row-1 committed files validate against their schemas using the pinned toolchain (`jsonschema == 4.23.0`, §8.1), hermetically (no network retrieval of meta-schemas).
- Evidence: recorded validation run of the committed governed files inside the pinned toolchain; PR record with the three review axes and Quality Gate verdict.

### Row 8 — Governance registry, routing, declaration schemas (§3 W5)

Checkpoint position: after row 2 (amendment §3 edge #2: 8 ← 2).

- Records: T06 #8 d2–d3 (registry and routing), d5 (fail-closed default).
- Scope: `governance/ownership/{principals,review-classes}/` with the slice principals and classes; `governance/change-declarations/` schemas for the three declaration kinds (schema content authored once in row 2, placed and wired here); the routing table; the fail-closed default.
- Acceptance criteria:
  - [ ] Registry content covers exactly the slice principals and review classes per T06 #8 d2–d3.
  - [ ] The routing table resolves every governed path class; unrouted paths fail closed (T06 #8 d5).
  - [ ] Declaration schemas agree byte-for-byte with the row-2 schema content; the configuration contract prevails over both.
- Evidence: PR record with the three review axes and Quality Gate verdict; routing-table coverage demonstration in tests.

## Exclusions

Validator and code families (R2); classifier/generator/views (R3); CI workflows (R4); Terraform (R5); all live activation (R6–R8). Nothing here contacts AWS, runs Terraform, or configures GitHub.

## Ordered checkpoints

Row 1 → Row 2 → Row 8 (amendment §3 edges #1–#2). A blocked row's acceptance criteria may not be evaluated as passing before its blocking row's criteria pass.

## Closeout rule

One closeout covers rows 1, 2, and 8 only when every row's acceptance criteria and required verification evidence pass together; any incomplete row keeps this ticket open (execution-grouping amendment §1).

## Lifecycle and review

Each row lands through the decision-gated implementation lifecycle S7–S11: `implement` (feature branch; never self-merges) → `codebase-design` (validate mode where the specification pre-settled structure) → `tdd` (red-first record kept; ratified seams only) → `code-review` with the Standards + Spec + Conformance axes → `code-review` as the independent Quality Gate; merge only at ⟦G-Merge⟧ by the repository owner. Unratified seams or questions HALT to excursion E1 (halt-don't-decide). During the recorded T15 d15 procedural interval (before the R6/14a control set is active), every implementation PR keeps PR/merge records and validation evidence procedurally.

## Authorization boundaries

Feature branches only; owner merges. No AWS contact or mutation; no Terraform execution; no GitHub configuration. Nothing in this ticket authorizes R6, R7, or R8.

## Empirical gates

None specific to these rows. The three open empirical conditions (provider execution; `forget` representation; import redaction) remain open, unadvanced, and are never claimed closed (specification §8.3).

## Blocked by

None — can start immediately (initial frontier).
