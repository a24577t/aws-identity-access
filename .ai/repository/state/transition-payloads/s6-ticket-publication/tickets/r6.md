## Source

- Specification: `docs/specifications/slice-a-engineering-specification.md` §10 row 14a; sequencing rules (correction-4 reconciliation).
- Grouping: `docs/specifications/slice-a-execution-grouping-amendment.md` row R6 (row 14a; no in-ticket checkpoints).
- Deciding records: T15 #10 d15 (planning lifecycle and the minimum server-enforced control set, configured under separate authorization before any apply; lab-only review exception record and its expiry terms).

**This ticket is separately authorized beyond its creation. Creating or claiming this issue authorizes nothing; every configuration step below requires Eric's explicit authorization at execution time.**

## What to activate

### Row 14a — Repository-side control activation (ruleset, required checks, environments)

Server-side activation of the T15 d15 minimum server-enforced set, only after the checks it requires exist on `main` (row 11 merged — carried by the native edge R6 ← R4):

- The `main` ruleset: requires pull requests; blocks direct push, force-push, and deletion; empty bypass list; requires the status checks `validate` and `plan-preview`.
- `plan-preview`: runs for the reviewed pull request; sanitized and non-authoritative; never eligible for apply.
- The `lab-plan` environment: requires `a24577t` approval; restricted to the `main` workflow path; produces the encrypted applicable saved plan and its digest from the exact `main` source commit.
- The `lab` environment: requires `a24577t` approval; restricted to `main`; may consume only that exact saved plan, source commit, inventory snapshot, and digest; any source, input, pin, inventory, enforcement-evidence, or plan change invalidates the authorization.
- Both environments disallow protection-rule bypass; deployment concurrency remains one.
- Self-review prevention remains disabled and is recorded as reduced lab assurance, enabled as soon as an independent reviewer exists.

- Acceptance criteria:
  - [ ] Each control above is live and matches T15 d15 exactly, verified from server state (rulesets and environments read back via the API), not from intent.
  - [ ] The exception-record expiry triggers (T15 d15: phase gate, or any change of visibility, plan, collaborator topology, branch rules, or deployment scope) are re-checked and still unfired at activation time.
  - [ ] The recorded T15 d15 procedural interval ends: from activation, server enforcement replaces procedural coverage for the named controls.
- Evidence: post-activation server-state capture (ruleset and environment configurations) preserved as the activation record; PR/record trail per the lifecycle below for any repository-side artifacts.

## Exclusions

AWS-side roles/OIDC/bootstrap (R7 — independently and separately authorized; **14a and 14b are not one atomic dependency**); lab remediation (R8); any Terraform execution; any AWS contact.

## Ordered checkpoints

Single row — none. Cross-ticket ordering is carried by the native edge R6 ← R4.

## Closeout rule

One closeout covers row 14a only when its acceptance criteria and activation evidence pass (execution-grouping amendment §1).

## Lifecycle and review

The live configuration steps are executed by the repository owner under their own separate authorizations. Repository-side records and evidence land through the decision-gated implementation lifecycle S7–S11 with the Standards + Spec + Conformance axes and the independent Quality Gate; merge only at ⟦G-Merge⟧ by the repository owner.

## Authorization boundaries

Separately authorized beyond ticket creation — no configuration change occurs from this ticket alone. No AWS contact or mutation; no Terraform execution. **No Terraform apply may occur anywhere before this row's control set is active** (T15 d15's planning lifecycle) — this row is the gate other work waits on, and activating it does not itself authorize any apply.

## Empirical gates

The three open empirical conditions (provider execution; `forget` representation; import redaction) remain open and unadvanced; activation of repository-side controls makes no claim about any of them (§8.3).

## Blocked by

- {{R4}} (R4 — CI workflows)
