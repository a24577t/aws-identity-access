## Source

- Specification: `docs/specifications/slice-a-engineering-specification.md` §10 row 13; contract §3 W8; §8.1 (Terraform/provider pins); §8.3.
- Grouping: `docs/specifications/slice-a-execution-grouping-amendment.md` row R5 (row 13; no in-ticket checkpoints).
- Deciding records: T15 #10 (state/evidence-prefix, role, deny, and pin contract; deployed-name derivation); T16 #11 (topology the roots target); T21 #20 F2 (provider lock `h1`); ADR-0009 (POC-managed resource set and protected resources).

## What to build

### Row 13 — Terraform roots + deployed-name derivation + role/deny model (§3 W8)

The `infrastructure/` Terraform roots `bootstrap` and `identity-center` under the T15 #10 state/evidence-prefix, role, deny, and pin contract, with deployed-name derivation `ialab-<key>` — **authoring only; zero Terraform execution**. The cross-ticket prerequisite (spec edge 13 ← 1) is satisfied by this ticket's native blocker R1.

- Scope: the two roots; the deployed-name derivation from the §9 canonical keys; the role/deny model exactly as T15 #10 d6/d7 records it (plan role and apply role access models; explicit denies; protected resources per ADR-0009); the committed `.terraform.lock.hcl` carrying exactly the pinned provider entry (`hashicorp/aws = 6.53.0`, `h1:eD0xCJQCp+iQQKpU/SpMk/pGRrkF16UUJAEMCXvWCWo=` — T21 #20 F2, authored from the recorded pin, since no `terraform init` may run); Terraform `1.15.7` exact (T15 #10 d13).
- Acceptance criteria:
  - [ ] Both roots exist with the T15 #10 state-key and evidence-prefix layout; no other roots.
  - [ ] `.terraform.lock.hcl` carries exactly the §8.1 provider version and `h1` hash; Terraform version constraint is `1.15.7` exact.
  - [ ] Deployed-name derivation produces `ialab-<key>` for the §9 canonical keys; no live identifier, account ID, ARN, root e-mail, or live OU path appears anywhere — aliases only.
  - [ ] The role/deny model matches T15 #10 d6/d7: pipeline roles cannot mutate IAM roles/trust, the OIDC provider, Organizations, S3 control plane, or KMS control plane; protected resources per ADR-0009 are outside every allow.
  - [ ] No `terraform init/plan/apply` output, state file, or provider binary is committed or was executed to produce this row.
- Evidence: PR record with the three review axes and Quality Gate verdict; static review against T15 #10 d6/d7 and §8.1; explicit record that no Terraform execution occurred.

## Exclusions

AWS-side bootstrap/apply of these roots (R7 — separately authorized); repository-side control activation (R6); lab remediation (R8); any AWS contact; any Terraform execution anywhere.

## Ordered checkpoints

Single row — none. Cross-ticket ordering is carried by the native edge R5 ← R1.

## Closeout rule

One closeout covers row 13 only when its acceptance criteria and required verification evidence pass (execution-grouping amendment §1).

## Lifecycle and review

Row 13 lands through the decision-gated implementation lifecycle S7–S11: `implement` (feature branch; never self-merges) → `codebase-design` (validate mode) → `tdd` (static/unit level — no provider execution) → `code-review` with the Standards + Spec + Conformance axes → `code-review` as the independent Quality Gate; merge only at ⟦G-Merge⟧ by the repository owner. Unratified seams or questions HALT to excursion E1. During the recorded T15 d15 procedural interval, every implementation PR keeps PR/merge records and validation evidence procedurally.

## Authorization boundaries

Feature branches only; owner merges. No AWS contact or mutation. **No Terraform execution of any kind** — `init`, `plan`, and `apply` are all out of scope; the first apply of any root is R7's separately authorized, owner-executed bootstrap, and no Terraform apply may occur before the R6/14a control set is active (T15 d15). No GitHub configuration. Nothing in this ticket authorizes R6, R7, or R8.

## Empirical gates

Provider execution is NOT RUN/BLOCKED on the authoring host and unverified until executed in the designated lab-CI boundary (§8.3) — this row is authored without executing the provider and makes no verification claim. The `forget`-representation and import-redaction conditions remain open and unadvanced.

## Blocked by

- {{R1}} (R1 — Foundation and contracts)
