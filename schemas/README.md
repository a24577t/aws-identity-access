# STUB — schemas/ (T04 layout prototype, throwaway)

- RD-04 (`02-configuration-model.md`) requires JSON Schema: JSON Schemas in this repository's
  `schemas/` directory validate every governed `.yml` file in CI; one resource per file.
- Anticipated schema subjects for slice A: instance, group reference, permission set, and
  account assignment.
- T14 owns the exact filenames, the JSON Schema dialect/version, the validation content, and
  the error codes (with T05, T21, T10, and T22 as inputs). No schema file is stubbed here.
