{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Change declaration - group-key-rename",
  "description": "governance/change-declarations/<declaration-key>.yml with kind: group-key-rename - governed intent for the exceptional atomic workforce-group stable-key rename (T05 #7 d4; T06 #8 d5), never approval evidence. The expected plan effect is fixed to zero AWS mutations; a rename plan containing any AWS mutation is rejected at the plan gate (GOV-DECL-MATCH). No approved_by, approval timestamps, GroupIds, ARNs, or account IDs (GOV-DECLARATION).",
  "type": "object",
  "properties": {
    "schema_version": {
      "description": "Declaration schema version (T06 #8 d5); the current version is 1 and changes atomically with this schema (T14 #19 d8).",
      "const": 1
    },
    "key": {
      "description": "Stable declaration key; equals the filename stem exactly.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$"
    },
    "kind": { "const": "group-key-rename" },
    "owner": {
      "description": "A T06 #8 principal key (declared ownership, not approval).",
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$"
    },
    "justification": { "type": "string", "minLength": 1 },
    "change_reference": {
      "description": "Immutable reference to the change this declaration authorizes (issue/PR URL or equivalent immutable identifier).",
      "type": "string",
      "minLength": 1
    },
    "deployment_scope": {
      "description": "The permitted deployment scope of the exceptional operation.",
      "type": "string",
      "minLength": 1
    },
    "valid_from": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$"
    },
    "valid_until": {
      "description": "Mandatory; indefinite declarations are prohibited and expired declarations fail closed (T06 #8 d5).",
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$"
    },
    "affected_paths": {
      "description": "The affected assignment-reference scope: every governed path the atomic rename touches.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "expected_plan_effect_class": {
      "description": "The T20 #22 d5 fixed class for the group-key-rename declared-change PR: empty - realizing T06 #8 d5's zero AWS mutations (declaration-vocabulary amendment, correction D1).",
      "const": "empty"
    },
    "lab_exception_reference": {
      "description": "Applicable lab-exception reference, if any (T06 #8 d5).",
      "type": "string",
      "minLength": 1
    },
    "from_key": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$",
      "minLength": 2,
      "maxLength": 64
    },
    "to_key": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$",
      "minLength": 2,
      "maxLength": 64
    }
  },
  "required": [
    "schema_version",
    "key",
    "kind",
    "owner",
    "justification",
    "change_reference",
    "deployment_scope",
    "valid_from",
    "valid_until",
    "affected_paths",
    "expected_plan_effect_class",
    "from_key",
    "to_key"
  ],
  "additionalProperties": false
}
